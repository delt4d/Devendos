@if (!_firstLoad)
{
    <MudProgressCircular 
        Indeterminate="true" 
        Color="Color.Primary" 
        Size="Size.Large" />
}
else
{
    <ContactList 
        OnContactInfoChanged="async (contact) => await ContactInfoChanged(contact)"
        Contacts="_searchService.FilteredContacts" 
    />
}